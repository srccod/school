# Ignore git metadata
.git
.gitignore

# Node and build artifacts
node_modules
dist
build

# Editor/IDE
.vscode
.idea

# OS files
.DS_Store
Thumbs.db

# Dockerfiles should be included in the build context (don't ignore)

# Environment files (don't include secrets in images)
.env

# Drizzle migration snapshots / metadata we don't want copied into images
server/src/db/migrations/meta/
server/src/db/migrations/**/meta/
server/src/db/migrations/*_journal.json
server/src/db/migrations/*_snapshot.json

# Temporary/dev artifacts
.cache
coverage
npm-debug.log
yarn-error.log
