yourdomain.com {
    # Serve the static frontend files from this directory
    root * /usr/share/caddy
    file_server

    # Reverse proxy API requests to the backend service
    handle_path /api/* {
        reverse_proxy backend:3001
    }
}
